# Attribution

This is independently written experimental prototype code inspired by the user's NavSentinel project and the primary sources in `docs/SOURCE-GROUNDING.md`. It is not an official upstream release or a repackaging of the complete repository. Upstream code, documents, names and third-party dependencies retain their own licenses and ownership. No font files or third-party runtime binaries are distributed in this bundle.

The custom interface graphics are inline SVG and simple locally generated application icons. The optional Electron dependency is downloaded separately through npm under its own license. Python Playwright is an optional external testing dependency, not included in the bundle.
