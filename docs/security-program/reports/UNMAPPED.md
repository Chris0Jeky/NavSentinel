# Unmapped and deliberately unclaimed

## Existing programme gaps

- RW-02, fake download CTA over legitimate content, has no current dedicated fixture/test vertical.
- RW-05, multi-step ad chain, has no current dedicated fixture/test vertical.
- The source tree has broader unit-level seams than the 63 journey and lane mappings. Capability-to-source coverage is recorded in [CAPABILITY_MATRIX.md](../CAPABILITY_MATRIX.md); this seed does not pretend each source file proves a scenario.

## Evidence not promoted

- All 168 canonical imported scenario snapshots remain `UNMODELLED`. Repository-current capability and evidence mappings record later receipts without rewriting the provenance-locked source catalogue.
- Existing Gym, RW, evasion, ClickFix, and DoubleClickjacking fixtures are mapped but generally remain `MODELLED` because many tests include product UI/events in the pass condition or lack a no-product fixture receipt.
- Twenty-one mapped legacy Gym pages remain excluded from programme evidence by [LEGACY_FIXTURE_SAFETY.md](LEGACY_FIXTURE_SAFETY.md). The four ClickFix fixtures are localized and no longer quarantined.
- The corpus result is `INVALID`; the false-positive result is `STALE`; the live-site and local benchmark lanes are `UNVERIFIED`.
- No current owner Chrome, human Gate-3, open-web efficacy, external review, native-companion, or universal-coverage evidence exists.
- No claim is made for anonymity, shell/OS enforcement, arbitrary credential-script exfiltration, all OAuth protocol failures, all extension conflicts, or all network consequences.

## Missing supplied material

The source bundle omitted advertised canonical capability, evidence-state, backlog, schema, and broader artifact inputs. See [SOURCE_PROVENANCE.json](../registry/SOURCE_PROVENANCE.json). A later full bundle must be reconciled by stable ID and cannot silently overwrite repository-current evidence or issue mappings.
