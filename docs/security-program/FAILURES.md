# Programme failures and invalid evidence

| ID | State | Failure | Operational consequence | Resume point |
| --- | --- | --- | --- | --- |
| SP-F-001 | OPEN | The supplied bundle contains 7 files while describing a 268-file source set; advertised canonical registries and schemas are absent. | Upstream validation claims for missing inputs are not independently reproducible. | Obtain the omitted source set, compare stable IDs and semantics, then re-import without silently replacing local reconciliation. |
| SP-F-002 | OPEN | The historical corpus result lacks real-host routing, trusted input, a protected-vs-fired oracle, and a committed manifest. | The 28% result is `INVALID` and cannot support efficacy or release claims. | Continue #417, then run through #416/#426 only after methodology validation. |
| SP-F-003 | OPEN | The historical false-positive result has no current reproducible input/output set. | The 0.72% result is `STALE`. | Establish a versioned benign manifest and rerun under the current release profile. |
| SP-F-004 | OPEN | The headed live-site lane is network-dependent and no current owner-Chrome or Gate-3 receipt exists. | Bundled Chromium and the live Google check cannot promote evidence to `BROWSER_PROVEN`. | Reuse #420 for the bounded maintainer-machine run and #439 for later Proving Ground work. |
| SP-F-005 | OPEN | The local benchmark uses product events as its main oracle and has no comparator arm. | It is `UNVERIFIED` and cannot support efficacy claims. | Define an independent sink and pinned comparator methodology before using its output. |
| SP-F-006 | OPEN | RW-02 and RW-05 are described in the older RW programme but have no current fixture/test vertical. | They remain unmapped and make no evidence contribution. | Deduplicate against the stable scenario registry before scheduling either journey. |
| SP-F-007 | RESOLVED | The mapped legacy Gym pages originally contained non-local destinations. | Four ClickFix, twelve evasion, seven core-level, and two RW fixtures now use typed local or inert destinations; no mapped fixture remains safety-held. Only the representative evasion journey has an independent harm receipt, and the RW journeys remain MODELLED. | Preserve the bounded local-target contract and do not promote provider, open-web, or mutation efficacy from these synthetic journeys. |
| SP-F-008 | RESOLVED | The first startup-egress repair relied on a browser proxy bypass that still routed loopback traffic and made the trial receipts `TEST_INVALID`. | The failed trial receipts were discarded; the local deny proxy prevented every attempted remote connection. | The fence now starts before Chromium, forwards only declared loopback HTTP origins, rejects every other request, and retains minimized blocked-attempt metadata. |
| SP-F-009 | OPEN | Native modified anchors mint no opener service-worker or MAIN open or redirect allowance. | Base-target, named-context, same-site, non-HTTP, and child-frame handlers remain reachable, while their same-task and deferred page popups are blocked as a known MEDIUM false intervention. | Reuse #566 only if this compatibility residual is scheduled. Preserve verified bridge identity and exact actor, event, frame, document, destination, TTL, and use-count scope; do not grant broad early-event authority or trust a page-origin signal. |
| SP-F-010 | RESOLVED | PR #572 passed the release budget but exceeded the unchanged research-profile `capture_isolated` limit by 516 bytes at `c6fd06d`. | Hosted run `33317574273` correctly stopped before E2E and release, so that head supplied no clean full-suite receipt. | Head `03f04f4` splits the shared modifier classifier and research-only child reputation path into separately capped chunks. Local release and research checks pass 13/13 and 16/16 budgets without raising a limit; retain both profile checks. |
| SP-F-011 | RESOLVED | PR #572 runs `33319338777` and `33320724757` exposed invalid rollback-test assumptions: a one-shot receipt, a direct `page.evaluate` bound to a replaced document, and generic page-event attribution that could select extension onboarding instead of the requested local child. | Both hosted heads correctly remained red. Those failures were `TEST_INVALID` or `HARNESS_INVALID`, not product containment receipts. | The fixture keeps the synthetic receipt durable, the rollback assertion is web-first across document replacement, and child selection now requires the exact loopback destination URL and exact added-page identity. Retain exact opener URL, history, child URL, receipt, and page-count assertions together. |
| SP-F-012 | OPEN | The service worker deliberately accepts rare possible rollback redelivery to avoid a missed rollback after worker death. | One diagnostic repeat contained the harmful local destination without history growth or extra children but delivered the return navigation twice, which can reload the restored page twice. | Reuse #566 only if an exactly-once lifecycle guarantee is scheduled. Preserve restart durability and add a separate reload-count oracle before changing delivery semantics. |
| SP-F-013 | OPEN | Two separate 50-profile campaigns for #566 each left the opener at the local `base-replace-handler` destination once. A Chrome-owned source-baseline acknowledgement candidate still produced one survivor in 50 and also exceeded the unchanged research `capture_isolated` budget. | A valid adjacent mutation still reaches the primary harm boundary, so PR #572 is parked before AI-31 and the mapped lane is only `FIXTURE_PROVEN`. The candidate is not pushed and no merge or browser gate is eligible. | Resume #566 by capturing the exact service-worker lifecycle, `lastUrl`, pending-rollback, delivery, and document-transition sequence for the retained seed. Minimize the first missing transition, preserve the unseen related holdout, and do not grant modified-anchor opener authority or raise a bundle budget. |
| SP-F-014 | PARTIAL | `BROWSER_LIMITATION`: the earliest normal authored-page #186 peer sent an init but received no MAIN-world challenge in ten fresh bundled-Chromium profiles; the real extension bridge became ready every time. | The structural port-possession concern remains `MODELLED`, but the harmful `02-ATTACK` baseline is not reachable with this authored-page ordering. A privileged add-init-script or CDP injection would be `TEST_INVALID` for page authority and cannot promote the evidence. | Establish a genuinely page-reachable same-session disclosure or pre-page actor without extra harness authority, then rerun the local DOM harm oracle. Otherwise proceed to an SW-vouched design only as an explicitly reviewed protocol slice; document-start timing is not identity authentication. |
| SP-F-015 | RESOLVED | `HARNESS_INVALID`: the first #186 receipt constrained its authored origin to loopback but did not install a request observer or deny proxy for Chromium or extension background traffic. | The unfenced network fields were discarded. Exact-head rerun `cc12019` used the pre-launch Proving Ground proxy, recorded zero fixture network violations, and retained 139 denied Chromium platform attempts; PR #603 merged the repair. | Keep the fence pre-launch and fail closed on any fixture network violation; do not reuse the superseded PR #602 receipt hash. |
