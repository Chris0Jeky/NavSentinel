# Agent Failure Ledger

This file is retained as a static historical view of recurring agent, tool, and
workflow failures. The repository-local auto-capture and renderer were retired
by owner decision #499. Add a curated entry manually only when the history is
still useful; this ledger is not a gate or required handoff artifact.

> **Scope correction (2026-08-02):** rows about PR #457, AI-18, a vendored
> floor, or a "next canonical floor sync" describe the retired repository-local
> harness. PR #457 later merged, AI-18 became obsolete, and #499 removed that
> entire NavSentinel surface. Their historical `open` labels do not create a
> current NavSentinel action. Any still-relevant upstream floor issue must be
> re-verified in the agent-harness repository; it will not arrive here through
> another local sync.

## Entries

| Date | Class | Surface | Failure | Workaround | Future fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-11 | pre_existing_noise | Bash | Backslash path separator mangled branch:path ref | Use forward slashes for git refs on Windows | n/a — agent tool misuse | closed |
| 2026-05-11 | pre_existing_noise | Bash | $null is PowerShell; bash uses /dev/null | Use 2>/dev/null in bash | n/a — agent tool misuse | closed |
| 2026-05-11 | pre_existing_noise | Bash | findstr treated unquoted arg as filename | Use grep -i in bash | n/a — agent tool misuse | closed |
| 2026-05-12 | non_blocking_risk | Bash | Script removed from package.json during release infra commit | Re-added to package.json | Fixed — scripts restored | closed |
| 2026-05-16 | non_blocking_risk | dependency | Dev server allows any website to read responses. Requires vite >=6.2 for fix, currently on 5.4. Dev-only — no production impact on built extension. | Do not browse untrusted pages while npm run dev or gym:serve is active | DONE: toolchain migrated to vite ^8 / vitest ^4 (cycles 6-7); esbuild GHSA-67mh-4wv8-2f99 (vite <6.2) no longer applies. | fixed |
| 2026-05-30 | invalid_signal | npm script | Exit code 1: PreToolUse guardrails did not deny git reset --hard. The hook is branch-aware and allows this command on non-protected branches to flow to the norm... | Interpret the pickup green hook-smoke state against protected main, or run smoke from a clean main worktree. D-STORE verification remains covered by typecheck, ... | DONE: smoke_test.py test_pre_tool_use is now branch-aware (splits always-deny vs branch-aware deny; asserts allow on non-protected branches). | fixed |
| 2026-06-05 | blocker | extension runtime | Plain assignment to the now-non-writable prototype.submit throws (Cannot assign to read only property submit), aborting JS-behavior init: lost signals + page er... | Guard the assignment with try/catch + graceful degrade (fixed in #185, commit 9da8bcc, merged 2026-06-05). | MAIN-world prototype patches must be try/catch-guarded; audit other prototype writes for patch-order assumptions. | fixed |
| 2026-06-13 | non_blocking_risk | npm script / CI | capture_isolated content-script chunk exceeded its per-chunk size budget after P5-C1 (#238) wiring; caught only in CI Build/Unit (the perf-budget step is NOT pa... | Moved the pure feature-builder into the storage chunk; bumped capture_isolated 61->62KB with documented justification; ran check:perf-budget locally to confirm. | Run check:perf-budget locally before pushing extension changes; consider folding it into the default test gate or a pre-push hook so it is not a CI-only surpris... | open |
| 2026-06-13 | non_blocking_risk | npm script / vite CLI | CACError: Unknown option --root. The vite 8 migration (cycles 6-7) dropped the --root CLI flag; vite 8 takes root as a POSITIONAL arg (vite [root]). gym:serve w... | Changed gym:serve to vite gym --port 5173 --strictPort (positional root). Verified the dev server starts and serves http://localhost:5173 (HTTP 200). | After a major build-tool upgrade, audit ALL CLI invocations in package.json scripts against the new tool help (npx vite --help), not just the build path. Remove... | fixed |
| 2026-06-14 | non_blocking_risk | multi-agent review tooling | Both subagents errored with Codex usage-limit exhaustion before producing review findings, so their runs could not satisfy the independent-review evidence gate. | Closed out the failed delegation path, performed direct local adversarial review of #253's changed seams, and recorded the failure explicitly instead of treatin... | Before depending on delegated reviews for a gate, confirm available Codex review/subagent quota or use a non-quota local review path; keep PR comments explicit ... | open |
| 2026-06-14 | non_blocking_risk | Codex shell / PowerShell | The active PowerShell version rejected `&&` as a statement separator, so the combined commit/push command failed before staging or committing docs. | Use separate PowerShell commands or semicolon-separated commands in this workspace; rerun the Git steps without POSIX-style `&&` chaining. | Prefer native PowerShell command sequencing in agent instructions and avoid POSIX shell separators when the environment context says `shell: powershell`. | open |
| 2026-06-14 | non_blocking_risk | GitHub CLI / multi-worktree merge | The GitHub CLI attempted a local git operation and failed because `main` was already checked out in a separate worktree, even though the PR itself was mergeable... | Merged PR #253 through the GitHub API merge endpoint, deleted the remote feature branch through the refs API, then removed the temporary helper worktree and fas... | When merging from a branch worktree while `main` is checked out elsewhere, either run `gh pr merge` from the `main` worktree or use the GitHub API merge endpoin... | open |
| 2026-07-03 | non_blocking_risk | GitHub PR / issue auto-close | GitHub's closing-keyword parser ignores negation: the phrase 'Does not close #418' in the PR body matched 'close #418' and auto-closed issue #418 on merge, desp... | Reopened #418 with a comment explaining the accidental auto-close and its remaining gated (Safe-Browsing comparison arm) scope. | Never place a closing keyword (close/closes/fix/fixes/resolve/resolves) adjacent to an issue number in a PR body or commit unless you intend the merge to close ... | resolved |
| 2026-07-12 / 2026-08-29 | non_blocking_risk | Windows Defender / adversarial test fixture | Defender detected `Trojan:HTML/FakeCaptcha.HNA!MTB` when agents read the tracked ClickFix property-test fixture. The 2026-08-29 recurrence came from an unrelated agent's broad recursive search across the shared `codex-shell-home`, not from NavSentinel test execution; Defender quarantined copies in preserved worktrees while the primary checkout remained intact. | Avoid scanner bypasses, exclusions, and allow rules. Keep cross-repository searches scoped to named paths that do not read the flagged fixture, and treat quarantine deletions in preserved worktrees as user work until reviewed. | AI-20 left the signature-shaped exact fixture quarantined and retained runtime-equivalent scan-clean coverage. A Microsoft false-positive report was submitted on 2026-08-29; no owner action is currently required, but keep this entry open until Microsoft clears the detection or a scan-clean tracked representation lands. | open |
| 2026-07-12 | non_blocking_risk | agent hook smoke / Git worktree path | post_tool_use.py split absolute edit paths on the literal NavSentinel/ basename, so an alternate worktree such as NavSentinel-ri01 did not trigger the agentic-c... | Ran the hook smoke from the canonical checkout while preserving the failure as an explicit follow-up. | FIXED: normalize absolute paths relative to the discovered repository root and exercise an alternate-named worktree path in agent:hooks:smoke. | fixed |
| 2026-07-13 | blocker | Claude/Codex irreversible-command floor | The local matrix passed while the deployed project floor allowed canonical-deny forms including git -C/git.exe/env-wrapped force-push, home-root deletion, prote... | Treat the exact global agent-harness e8f79a7 v1.4.1 dispatcher as the only runtime floor source; keep repo copies only as pinned CI fixtures; do not rely on Cod... | Complete infra/harness-deny-floor-v141: one floor per runtime, exact identity pins, Windows/Linux CI smoke and skill gates, two independent reviews, then human ... | open |
| 2026-07-14 | blocker | agent-harness v1.4.1 exact-head review | Late bot and independent reviews invalidated the prior v1.4.1 approval: five parser paths failed open, late sensitive-push cases were counted but not asserted, ... | Invalidated every earlier exact-head review, reopened the canonical source, tracked the wave in agent-harness issue #7, and kept PR #457 draft and AI-18 trust b... | Finish two fresh reviews of canonical ba0f67d and the final PR #457 successor head, resolve every bot thread, verify Windows/Linux CI and exact identities, then... | open |
| 2026-07-17 | non_blocking_risk | MV3 emitted-worker release gate | The emitted-worker gate successively missed comment-separated module edges and then accepted Chrome-unsupported static import attributes/assertions, so build/pa... | Replaced edge regexes with es-module-lexer, added a TypeScript syntax pass for unsupported import attributes/assertions and invalid JavaScript, and expanded the... | For MV3 capability gates, enumerate unsupported syntax in fixtures and use syntax-aware parsing for parser-sensitive properties; keep the exact emitted-output g... | fixed |
| 2026-07-24 | non_blocking_risk | vendored deny floor (.claude/hooks/dispatch.py 1.5.2) | The vendored floor denies a charter command bare but ALLOWS the same command wrapped in any of three PowerShell one-liner shapes, so it contradicts its own verd... | Branch protection is NOT the mitigation. AI-17 covers only the 'git push --force' class; a wrapped 'rm -rf' outside the project and a wrapped download piped to ... | Owned upstream by agent-harness issue #37 (reproduction evidence and a version-agnostic repro script posted there 2026-07-24). When a canonical floor release cl... | open |
| 2026-07-24 | non_blocking_risk | vendored deny floor / Bash git plumbing | Read-only git plumbing is denied as '[floor 1.5.2] An unknown git alias/subcommand is opaque to the deny floor.' The floor allowlists known subcommands and deni... | Use allowed equivalents, but note they are ONE-SIDED: 'git log --oneline main..<branch>' lists only commits the branch has and main lacks, so it cannot by itsel... | Owned upstream by agent-harness issue #34 (extend the known-subcommand set with read-only plumbing). Arrives in this repo with the next canonical floor sync. | open |
| 2026-07-24 | non_blocking_risk | vendored deny floor / GitHub CLI | A gh --body argument whose markdown contains an inline code span is denied when the span happens to hold something the floor recognises (an environment assignme... | Write the body to a file under the session scratchpad and use --body-file. Used successfully for the agent-harness #37 write-up on 2026-07-24. Prefer --body-fil... | Owned upstream by agent-harness issue #36 (honour POSIX backslash escaping inside double-quoted spans before command-substitution detection). Arrives with the n... | open |
| 2026-07-24 | non_blocking_risk | agent hook topology / PreToolUse | Two deny-floor PreToolUse hooks are wired at once, so each Bash call is judged twice by two different floor versions. Global ~/.claude/settings.json registers P... | None applied. Both floors deny a superset of what one would, so the topology is fail-safe rather than fail-open and does not block work. Do not delete the vendo... | FIXED in #457 (merged 2026-07-25 as dc3d0da). The repo-local PreToolUse hook was removed from .claude/settings.json, so Claude runs only the canonical global di... | fixed |
| 2026-07-25 | pre_existing_noise | npm run agent:hooks:smoke / vendored floor matrix | 'FAIL - deny floor matrix (canonical): Command [...python, .claude/hooks/smoke_test.py] timed out after 120 seconds'. The vendored floor's own smoke suite no lo... | Read the FULL smoke output, never a tail. This failure prints near the TOP of the run, so 'npm run agent:hooks:smoke \| tail -3' shows only trailing 'ok -' line... | FIXED in #457 (merged 2026-07-25). The per-subtest timeout in scripts/agent_hooks/smoke_test.py was raised 120s -> 600s, with a comment recording the measuremen... | fixed |
| 2026-07-25 | invalid_signal | local Playwright E2E (real extension, launchPersistentContext) | Local full-suite E2E reports failures that do not reproduce in CI or in isolation. Observed twice on 2026-07-25 while integrating #356/#464. (a) Default local c... | Run local E2E as 'CI=1 npm run test:e2e' so workers=1 and retries=1 match CI. Treat a local toast-timeout failure as unproven until checked two ways: re-run the... | Either lower the default local worker count for the extension-loading specs or raise the toast timeouts, so the default local invocation is not misleading. The ... | open |
| 2026-07-25 | non_blocking_risk | agent verification method / Playwright list reporter | A failing E2E run was initially read as clean. Two compounding causes. (1) Playwright's list reporter prints failures as '  N failed' followed by the failed tes... | Write the real exit code to a FILE, not stdout: 'cmd > run.log 2>&1; echo "exit=$?" > run.exit'. Then read run.exit, and count outcomes with the reporter's actu... | Prefer a machine-readable reporter for agent runs (--reporter=json or line) and assert on the parsed status counts rather than scraping human output. | resolved |

## Classification

- `blocker`: work cannot safely continue.
- `non_blocking_risk`: work can continue, but confidence or coverage is reduced.
- `pre_existing_noise`: unrelated existing failure that should still be visible.
- `invalid_signal`: false alarm, stale check, or non-applicable warning.

## Promotion Rule

A ledger entry should become a guide or skill update only when it is reproducible, project-specific, and likely to recur. Use `GUIDE_UPDATE_PROTOCOL.md`; do not mutate root instructions after a single ambiguous failure.
