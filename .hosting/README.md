# NavSentinel hosting and naming reference

Reference-only preparation, 2026-09-10. No executable prompt, agent hook, tier, lifecycle script or Harness CI is added. The owner's removal of the repository-local harness remains intact. `manifest.json` is inert metadata, not a capability or deployment grant.

The extension stays local. Its browser service worker is not a Cloudflare Worker. A public landing/privacy/support site can be static and does not require a reputation API, native host, remote analytics or a hosted protection runtime. No extension, hostname, name, account, telemetry or deployment configuration changes occur here. Existing workflows may still run after a future merge.

## Existing owner: AI-19

Continue the established AI-19 naming task rather than opening a competing decision process. The requested direction is rename preparation. Keep unregistered candidate names/domain quotes private until chosen and secured; this PR does not adopt a name.

Inventory display strings, manifest labels, icons, screenshots, privacy/support URLs and store copy separately from extension IDs, signing/store identity, storage keys, permissions, package coordinates and saved settings. A broad search-and-replace is not an acceptable migration. Preserve the interaction-only release profile, local storage, trusted-domain controls and existing browser behavior.

NS1-NS3 in the manifest define the rename impact map, truthful static-site surface and release-compatibility acceptance. Store or efficacy claims must reflect current verified behavior, not proposed security features. A new domain does not imply public release approval.

## Verification

Follow `CLAUDE.md`, `AGENTS.md`, the seam map and `ACTION_ITEMS.md`. Syntax: `python -m json.tool .hosting/manifest.json`. Subsequent manifest/bundling changes require the existing build/profile/version/package checks; actual browser-behavior changes additionally require their applicable tests and human Gate-3. Do not require a browser-behavior gate merely to validate these reference files.

Agents must not load or reload an unpacked extension through a workaround. The owner controls that step under the existing repository contract. Preserve a previous tested package and settings export before a later approved rebrand. Credentials and private operational receipts never belong here.
